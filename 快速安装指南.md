# 快速安装和运行指南

## 🎯 3分钟快速开始

### 步骤1: 克隆或下载项目

```bash
# 如果是从GitHub克隆
git clone [你的仓库地址]
cd RLBoss

# 或者直接下载并解压到 RLBoss 目录
```

### 步骤2: 安装依赖

```bash
# 使用pip安装所有依赖
pip install -r requirements.txt
```

**依赖清单**:
- `torch>=2.0.0` - 深度学习框架
- `gymnasium>=0.29.0` - 强化学习环境
- `matplotlib>=3.7.0` - 可视化
- `numpy>=1.24.0` - 数值计算

### 步骤3: 运行演示

```bash
# 运行快速演示（约2-3分钟）
python quick_start.py
```

**完成！** 你应该看到：
- 训练过程的实时输出
- 最终性能统计
- 结果保存位置提示

---

## 📋 详细安装步骤

### 方式1: 使用虚拟环境（推荐）

```bash
# 创建虚拟环境
python -m venv rlboss_env

# 激活虚拟环境
# Windows:
rlboss_env\Scripts\activate
# Linux/Mac:
source rlboss_env/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### 方式2: 使用Conda

```bash
# 创建conda环境
conda create -n rlboss python=3.9

# 激活环境
conda activate rlboss

# 安装PyTorch（根据你的CUDA版本）
# CPU版本:
conda install pytorch torchvision torchaudio cpuonly -c pytorch
# GPU版本（CUDA 11.8）:
conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia

# 安装其他依赖
pip install gymnasium[box2d] matplotlib numpy
```

### 方式3: 最小安装（仅核心功能）

```bash
# 只安装最基本的依赖
pip install torch numpy gymnasium
```

---

## 🔍 验证安装

### 检查Python版本

```bash
python --version
# 应该显示 Python 3.8 或更高版本
```

### 检查依赖

```python
# 运行以下Python代码检查
python -c "import torch; import gymnasium; import numpy; import matplotlib; print('所有依赖安装成功！')"
```

如果没有错误，说明安装成功！

### 运行简单测试

```bash
# 测试基本功能
python -c "from dual_policy_ppo import DualPolicyPPO; agent = DualPolicyPPO(4, 2); print('算法模块加载成功！')"
```

---

## 🚀 快速使用示例

### 示例1: 最简单的使用

```python
from dual_policy_ppo import DualPolicyPPO
from trainer import Trainer

# 创建智能体
agent = DualPolicyPPO(state_dim=4, action_dim=2, continuous=False)

# 创建训练器
trainer = Trainer('CartPole-v1', agent, max_episodes=100)

# 开始训练
history = trainer.train()
```

### 示例2: 命令行运行

```bash
# 在CartPole环境训练300回合
python experiment.py --env CartPole-v1 --max-episodes 300

# 在Pendulum环境训练（连续控制）
python experiment.py --env Pendulum-v1 --max-episodes 300 --intrinsic-coef 0.05

# 运行对比实验
python compare_experiments.py --env CartPole-v1 --n-runs 3
```

### 示例3: 自定义配置

```bash
# 使用自定义超参数
python experiment.py \
  --env CartPole-v1 \
  --max-episodes 500 \
  --intrinsic-coef 0.15 \
  --kl-coef 0.02 \
  --lr 0.0001
```

---

## 🛠️ 常见安装问题

### 问题1: PyTorch安装失败

**解决方案**:
```bash
# 方法1: 使用清华镜像源
pip install torch -i https://pypi.tuna.tsinghua.edu.cn/simple

# 方法2: 从官网下载对应版本
# 访问 https://pytorch.org/ 选择合适的版本
```

### 问题2: Gymnasium安装失败

**解决方案**:
```bash
# 如果box2d环境安装失败，可以不安装
pip install gymnasium

# 需要LunarLander环境时再安装
pip install gymnasium[box2d]
```

### 问题3: 缺少CUDA

**解决方案**:
```python
# 算法会自动使用CPU，性能稍慢但功能完整
# 如果需要GPU加速，安装CUDA版本的PyTorch
```

### 问题4: Matplotlib中文显示问题

**解决方案**:
```bash
# Windows: 确保安装了中文字体（SimHei或Microsoft YaHei）
# Linux: sudo apt-get install fonts-wqy-microhei
# Mac: 系统默认支持中文
```

---

## 📊 目录结构说明

安装完成后，你的目录应该是这样的：

```
RLBoss/
├── dual_policy_ppo.py       # ← 核心算法（从这里开始阅读）
├── trainer.py               # ← 训练器
├── experiment.py            # ← 运行单个实验
├── quick_start.py           # ← 快速开始（推荐第一个运行）
├── example_usage.py         # ← 使用示例集合
├── README.md                # ← 详细文档
├── requirements.txt         # ← 依赖列表
└── ...（其他文件）
```

**推荐阅读顺序**:
1. `README.md` - 了解算法原理
2. `quick_start.py` - 运行第一个例子
3. `example_usage.py` - 学习各种使用方式
4. `dual_policy_ppo.py` - 深入理解实现

---

## 🎮 运行你的第一个实验

### 选项1: 交互式快速开始

```bash
python quick_start.py
# 按提示操作即可
```

### 选项2: 自定义环境

```bash
python quick_start.py --custom
# 可以选择不同环境和参数
```

### 选项3: 查看所有示例

```bash
python example_usage.py
# 选择1-7任一示例运行
```

### 选项4: 运行完整实验

```bash
# 单个环境
python experiment.py --env CartPole-v1 --max-episodes 300

# 多个环境对比
python test_environments.py --env all
```

---

## 📈 查看结果

训练完成后，结果保存在 `results/` 目录下：

```
results/
└── [实验名称]/
    ├── best_model.pth           # 最佳模型
    ├── final_model.pth          # 最终模型
    ├── training_history.json    # 训练历史数据
    └── training_results.png     # 训练曲线图
```

### 可视化结果

```bash
# 查看单个实验结果
python visualize_results.py \
  --mode single \
  --history results/[实验名称]/training_history.json

# 对比多个实验
python visualize_results.py \
  --mode compare \
  --experiments exp1 exp2 exp3 \
  --results-dir ./results
```

---

## 🎓 下一步

安装完成后，你可以：

1. **学习理论**
   - 阅读 `README.md` 理解算法设计
   - 阅读 `ARCHITECTURE.md` 了解架构细节

2. **运行实验**
   - 尝试不同环境
   - 调整超参数
   - 对比不同算法

3. **修改代码**
   - 查看 `example_usage.py` 学习API
   - 修改 `dual_policy_ppo.py` 实现自己的想法
   - 添加新的环境和功能

4. **贡献代码**
   - Fork项目
   - 提交Pull Request
   - 分享你的改进

---

## 💡 小贴士

### 提升训练速度

```python
# 使用GPU（如果有）
agent = DualPolicyPPO(
    state_dim=4,
    action_dim=2,
    device='cuda'  # 或 'auto' 自动选择
)

# 减小网络大小
agent = DualPolicyPPO(
    state_dim=4,
    action_dim=2,
    hidden_dim=128  # 默认256，可以减小到128
)
```

### 调试技巧

```python
# 打印训练统计
trainer = Trainer(
    env_name='CartPole-v1',
    agent=agent,
    log_frequency=1  # 每回合都打印
)

# 快速测试（少量回合）
trainer = Trainer(
    env_name='CartPole-v1',
    agent=agent,
    max_episodes=10  # 快速测试
)
```

---

## 📞 获取帮助

遇到问题？

1. **查看文档**
   - `README.md` - 使用指南
   - `ARCHITECTURE.md` - 技术细节
   - `项目概览.md` - 项目全貌

2. **运行示例**
   - `python example_usage.py` - 查看各种用法

3. **提Issue**
   - 描述问题
   - 提供错误信息
   - 说明运行环境

---

<div align="center">

**🎉 安装完成！开始你的强化学习之旅吧！**

运行这个命令开始：
```bash
python quick_start.py
```

</div>

